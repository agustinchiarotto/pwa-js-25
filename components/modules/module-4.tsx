                      <div className="bg-zinc-800 text-zinc-200 p-4 rounded-md font-mono text-sm">
                        <pre>{`// Suma de todos los números
const suma = numeros.reduce((total, num) => total + num, 0);
console.log("Suma:", suma);

// Números mayores a 8
const mayoresA8 = numeros.filter(num => num > 8);
console.log("Mayores a 8:", mayoresA8);

// Multiplicar por 2
const duplicados = numeros.map(num => num * 2);
console.log("Duplicados:", duplicados);`}</pre>
                      </div> 